// (Intentionally empty.) Real type declarations come from `@types/node`,
// `@types/express`, and `fast-xml-parser`'s bundled types after `npm install`.
//
// During initial scaffolding (before npm install) this file briefly held
// hand-written ambient stubs so the project could be type-checked offline.
// They've been removed because they conflict with the real, more accurate
// types that npm provides.
export {};
